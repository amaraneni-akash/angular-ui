import { RectangularProgressBarDirective } from './rectangular-progress-bar.directive';

describe('RectangularProgressBarDirective', () => {
  it('should create an instance', () => {
    const directive = new RectangularProgressBarDirective();
    expect(directive).toBeTruthy();
  });
});
