import { VerticalBarChartDirective } from './vertical-bar-chart.directive';

describe('VerticalBarChartDirective', () => {
  it('should create an instance', () => {
    const directive = new VerticalBarChartDirective();
    expect(directive).toBeTruthy();
  });
});
