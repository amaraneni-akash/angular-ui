import { SpeedometerDirective } from './speedometer.directive';

describe('SpeedometerDirective', () => {
  it('should create an instance', () => {
    const directive = new SpeedometerDirective();
    expect(directive).toBeTruthy();
  });
});
